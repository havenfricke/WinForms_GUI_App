### C# Windows Forms + Arduino Serial Communication

This project demonstrates how to create a C# Windows Forms application that communicates directly with an Arduino device via serial communication. 
The application allows users to send commands to the Arduino and receive data from it.

This is still a work in progress, and I will be adding more features and improving the user interface over time....